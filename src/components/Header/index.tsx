import React from 'react';
import { PageHeader } from 'antd';

const Header = (): React.ReactElement => (
    <PageHeader
        className="site-page-header"
        title="{ Curly }"
        subTitle="Translate and store!"
    />
)

export default Header
